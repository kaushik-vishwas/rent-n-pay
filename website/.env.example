# Optional — only needed for local backend development.
# Without this file, the site uses https://backend.delicod.com/api (live).
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_API_URL=https://backend.delicod.com/api
