export const metadata = {
  title: 'Help Center | Rentnpay',
  description: 'FAQs, contact options, and your support tickets.',
};

export default function HelpCenterLayout({ children }) {
  return children;
}
